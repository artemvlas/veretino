# Script files must be executable, if it's not:
chmod +x scriptFileName.sh

# Scripts were created and tested on Arch Linux (or based).
# Other distros may have different command and package names, so some changes may need to be made.

# It is assumed that the "base-devel" and "cmake" packages are already installed on the system.
# If it's not, do:
sudo pacman -S base-devel cmake
